import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AddItemOrcamentoAcessorio2Page } from './add-item-orcamento-acessorio2.page';

describe('AddItemOrcamentoAcessorio2Page', () => {
  let component: AddItemOrcamentoAcessorio2Page;
  let fixture: ComponentFixture<AddItemOrcamentoAcessorio2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AddItemOrcamentoAcessorio2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
